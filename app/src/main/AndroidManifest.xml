<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="android_serialport_api.sample"
    android:versionCode="3"
    android:versionName="1.2">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:name=".Application"
        android:icon="@drawable/icon"
        android:label="@string/app_name"
        tools:ignore="GoogleAppIndexingWarning">
        <activity
            android:name=".MainMenu"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SerialPortPreferences" />
        <activity android:name=".ConsoleActivity" />
        <activity android:name=".LoopbackActivity" />
        <activity android:name=".Sending01010101Activity" />
        <activity android:name=".SerialPortTestActivity" />
        <activity android:name=".TestAllActivity" />
        <activity android:name=".TestPortActivity"></activity>
    </application>

</manifest>